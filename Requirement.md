# 笔记系统——需求文档与架构书


# 第一部分：需求文档（Software Requirements Specification）


## 1. 项目概述

### 1.1 项目背景

在数据隐私日益受到重视的当下，用户对笔记数据的自主控制需求显著增长。本项目旨在开发一款**自托管、多用户、端到端加密**的笔记系统，使个人和团队能够安全地记录、管理笔记，并通过Google Drive实现自动化异地备份。

### 1.2 项目目标

- 提供安全、私密的笔记管理体验，笔记内容在服务端加密存储
- 支持多用户独立账户，用户数据完全隔离
- 通过Web前端提供友好的笔记编辑界面
- 实现自动化数据库备份至Google Drive，保障数据安全

### 1.3 范围界定

| 范围内 | 范围外 |
| :--- | :--- |
| 用户注册、登录、登出 | 实时协同编辑 |
| 笔记的创建、编辑、删除、查看 | 移动端原生应用（初期） |
| 笔记列表展示与搜索 | 笔记分享/公开链接 |
| 服务端AES-256-GCM加密存储 | 图片/文件附件上传（初期） |
| Google Drive自动备份 | 富文本复杂排版（初期支持Markdown） |
| Docker容器化部署 | 多语言国际化 |


## 2. 用户角色

| 角色 | 描述 | 权限 |
| :--- | :--- | :--- |
| **普通用户** | 注册并登录系统的终端用户 | 管理自己的笔记（CRUD）、查看自己的笔记列表、搜索自己的笔记 |
| **系统管理员** | 系统运维人员 | 用户管理、系统配置、备份监控、日志查看 |


## 3. 功能需求

### 3.1 用户认证与多用户管理

| 编号 | 需求描述 | 优先级 |
| :--- | :--- | :--- |
| FR-01 | 用户可以通过邮箱/用户名注册新账户 | P0 |
| FR-02 | 用户可以使用凭证登录系统，会话通过JWT Token维持 | P0 |
| FR-03 | 用户可以主动登出系统 | P1 |
| FR-04 | 所有笔记操作必须验证当前用户的身份和所有权 | P0 |
| FR-05 | 不同用户的笔记数据完全隔离，用户只能看到自己的笔记 | P0 |

### 3.2 笔记管理

| 编号 | 需求描述 | 优先级 |
| :--- | :--- | :--- |
| FR-06 | 用户可创建新笔记，包含标题和正文内容 | P0 |
| FR-07 | 用户可编辑已有笔记的标题和正文 | P0 |
| FR-08 | 用户可删除自己的笔记（需确认步骤） | P0 |
| FR-09 | 用户可查看自己的笔记列表，按更新时间降序排列 | P0 |
| FR-10 | 用户可查看单篇笔记的完整内容 | P0 |
| FR-11 | 笔记编辑器支持Markdown语法 | P1 |
| FR-12 | 用户可按标题或正文内容搜索自己的笔记 | P1 |

### 3.3 数据加密

| 编号 | 需求描述 | 优先级 |
| :--- | :--- | :--- |
| FR-13 | 笔记内容在**服务端**使用AES-256-GCM算法加密后存入数据库 | P0 |
| FR-14 | 加密主密钥通过环境变量注入，不得硬编码在代码中 | P0 |
| FR-15 | 每个笔记加密时使用随机生成的Nonce（12字节），Nonce与密文一同存储 | P0 |
| FR-16 | 数据库仅存储加密后的密文，绝不存储明文笔记内容 | P0 |
| FR-17 | 笔记标题可选择是否加密（建议加密） | P1 |

### 3.4 Google Drive自动备份

| 编号 | 需求描述 | 优先级 |
| :--- | :--- | :--- |
| FR-18 | 系统支持配置Google Drive API凭证（服务账号方式） | P0 |
| FR-19 | 系统定期（如每日凌晨）自动导出完整数据库 | P0 |
| FR-20 | 导出的数据库文件经压缩后上传至指定的Google Drive文件夹 | P0 |
| FR-21 | 备份文件名包含时间戳，便于追溯 | P1 |
| FR-22 | 备份任务执行结果需记录日志，失败时触发告警 | P1 |


## 4. 非功能需求

### 4.1 性能

| 编号 | 需求描述 | 目标值 |
| :--- | :--- | :--- |
| NFR-01 | 后端API接口响应时间 | < 500ms |
| NFR-02 | 单用户支持笔记数量 | 10,000+ 条 |
| NFR-03 | 并发用户支持 | 初始支持50+并发用户 |

### 4.2 安全性

| 编号 | 需求描述 |
| :--- | :--- |
| NFR-04 | 全站启用HTTPS加密传输 |
| NFR-05 | 用户密码使用bcrypt或Argon2哈希存储 |
| NFR-06 | JWT Token设置合理过期时间（如7天） |
| NFR-07 | 加密密钥通过环境变量或密钥管理服务（KMS）管理 |
| NFR-08 | 数据库备份文件在传输和存储中保持加密状态 |

### 4.3 可用性与可维护性

| 编号 | 需求描述 |
| :--- | :--- |
| NFR-09 | 系统支持Docker容器化一键部署 |
| NFR-10 | 提供详细的部署文档和运维手册 |
| NFR-11 | 日志系统完善，便于问题排查 |
| NFR-12 | 前端界面响应式设计，适配桌面与平板设备 |

### 4.4 可扩展性

| 编号 | 需求描述 |
| :--- | :--- |
| NFR-13 | 架构设计支持后续横向扩展（多实例部署） |
| NFR-14 | 数据库设计支持后续迁移至PostgreSQL等生产级数据库 |


## 5. 用户流程

### 5.1 注册与登录流程

```
用户访问系统 → 进入登录页 → 无账户？→ 进入注册页
                ↓
           输入凭证登录 → 验证通过 → 跳转笔记列表
                ↓
           验证失败 → 显示错误提示
```

### 5.2 笔记操作流程

```
笔记列表页 → 点击"新建笔记" → 输入标题和内容 → 保存 → 返回列表
     ↓
点击已有笔记 → 查看详情 → 点击"编辑" → 修改内容 → 保存 → 返回详情
     ↓
点击"删除" → 确认对话框 → 确认 → 笔记删除 → 返回列表
```


# 第二部分：架构设计书（Architecture Design Document）


## 1. 架构概述

### 1.1 设计原则

- **安全优先**：数据加密贯穿存储与传输全链路
- **分层解耦**：各层职责清晰，便于独立演进
- **云原生就绪**：支持Docker容器化部署
- **数据主权**：用户数据完全归用户所有

### 1.2 整体架构图

```
┌─────────────────────────────────────────────────────────────────────────┐
│                            客户端层 (Client Layer)                     │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │                    Web 浏览器 (React / Vue)                     │   │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐      │   │
│  │  │ 登录/注册 │  │ 笔记列表 │  │ Markdown │  │  搜索栏  │      │   │
│  │  │   页面    │  │   页面   │  │  编辑器  │  │          │      │   │
│  │  └──────────┘  └──────────┘  └──────────┘  └──────────┘      │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                                    │ HTTPS (携带 JWT Token)           │
└────────────────────────────────────┼────────────────────────────────────┘
                                     ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                          接入与分发层 (Access Layer)                   │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │              反向代理 & 负载均衡 (Nginx / Caddy)                │   │
│  │                 - HTTPS 终止  - 静态资源服务                    │   │
│  └─────────────────────────────────────────────────────────────────┘   │
└────────────────────────────────────┼────────────────────────────────────┘
                                     ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                          应用服务层 (Application Layer)                │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │              后端应用服务 (FastAPI / Express)                   │   │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐         │   │
│  │  │  认证模块    │  │  笔记业务    │  │  加密模块    │         │   │
│  │  │ (JWT签发/验证)│  │  (CRUD操作)  │  │(AES-256-GCM) │         │   │
│  │  └──────────────┘  └──────────────┘  └──────────────┘         │   │
│  │  ┌──────────────┐  ┌──────────────┐                           │   │
│  │  │  备份调度器  │  │  Google Drive│                           │   │
│  │  │(Cron/Celery) │  │  API客户端   │                           │   │
│  │  └──────────────┘  └──────────────┘                           │   │
│  └─────────────────────────────────────────────────────────────────┘   │
└────────────────────────────────────┼────────────────────────────────────┘
                                     ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                          数据存储层 (Data Layer)                       │
│  ┌─────────────────────────┐    ┌─────────────────────────────────┐   │
│  │     主数据库            │    │      外部存储 (备份)            │   │
│  │  (PostgreSQL / SQLite)  │    │                                │   │
│  │  ┌───────────────────┐  │    │  ┌─────────────────────────┐  │   │
│  │  │  users 表         │  │    │  │    Google Drive         │  │   │
│  │  │  notes 表 (密文)  │  │◄───│──│  (加密数据库备份压缩包)  │  │   │
│  │  └───────────────────┘  │    │  └─────────────────────────┘  │   │
│  └─────────────────────────┘    └─────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────┘

                         安全与配置管理层 (贯穿全栈)
          ┌─────────────────────────────────────────────────────┐
          │  环境变量 / 密钥管理 (Master Key, OAuth 凭证)      │
          └─────────────────────────────────────────────────────┘
```


## 2. 技术栈选型

### 2.1 技术栈总览

| 层级 | 技术选型 | 选型理由 |
| :--- | :--- | :--- |
| **前端框架** | React + TypeScript | 生态成熟、类型安全、组件化开发 |
| **前端编辑器** | TipTap / Markdown编辑器 | 基于ProseMirror，灵活可扩展 |
| **后端框架** | Python FastAPI | 高性能、异步支持、自动生成API文档 |
| **数据库** | PostgreSQL（生产）/ SQLite（开发） | 功能强大、稳定可靠 |
| **加密库** | Python `cryptography` | 官方推荐、AES-256-GCM支持完善 |
| **认证方案** | JWT (JSON Web Token) | 无状态、易于扩展 |
| **云存储API** | Google Drive API (官方SDK) | 官方支持、文档完善 |
| **容器化** | Docker + Docker Compose | 环境一致性、一键部署 |
| **反向代理** | Nginx | 高性能、成熟稳定 |

### 2.2 技术选型备选方案

| 组件 | 主选方案 | 备选方案 |
| :--- | :--- | :--- |
| 前端框架 | React | Vue 3 |
| 后端框架 | FastAPI | Node.js Express / Django |
| 数据库 | PostgreSQL | MySQL |


## 3. 核心模块详细设计

### 3.1 认证模块

**职责**：处理用户注册、登录、会话管理

**技术实现**：
- 用户密码使用 `bcrypt` 哈希存储
- 登录成功后签发JWT Token，包含 `user_id` 和 `exp`（过期时间）
- Token通过 `Authorization: Bearer <token>` 头传递
- 所有受保护路由的请求首先验证JWT有效性

**数据流**：
```
注册请求 → 密码哈希 → 存入users表 → 返回成功
登录请求 → 验证密码 → 签发JWT → 返回Token
API请求 → 验证JWT → 提取user_id → 注入业务上下文
```

### 3.2 笔记管理模块

**职责**：处理笔记的创建、读取、更新、删除操作

**技术实现**：
- 所有操作均基于当前用户的 `user_id` 进行数据隔离
- 笔记内容在**入库前加密**，在**出库后解密**返回前端
- 查询列表时仅返回笔记元数据（id、标题、创建时间、更新时间），不返回正文内容

**数据模型**：

```python
# notes 表结构
{
    "id": "uuid",           # 笔记唯一标识
    "user_id": "uuid",      # 所属用户（外键）
    "title_encrypted": "text",   # 加密后的标题
    "content_encrypted": "text", # 加密后的正文
    "nonce": "text",        # 加密使用的Nonce（Base64编码）
    "created_at": "timestamp",
    "updated_at": "timestamp"
}
```

### 3.3 加密模块

**职责**：提供笔记内容的加密和解密能力

**算法选型**：**AES-256-GCM**

选型理由：
- GCM是认证加密（AEAD）模式，同时提供机密性和完整性验证
- 被广泛用于TLS等安全协议，经过充分验证
- Python `cryptography` 库提供原生支持

**加密流程**：
```
1. 从环境变量读取 Master Key（32字节）
2. 为每个笔记生成随机12字节Nonce
3. 使用 AES-256-GCM 加密明文内容
4. 输出：密文 + 认证标签（16字节）
5. 将密文和Nonce（Base64编码）存入数据库
```

**解密流程**：
```
1. 从数据库读取密文和Nonce
2. 使用相同的 Master Key 和 Nonce 解密
3. 验证认证标签
4. 返回明文内容
```

**安全要点**：
- **绝对不要重复使用Nonce**——同一Key下重复Nonce会彻底破坏安全性
- 密钥通过环境变量管理，与代码仓库分离
- 建议每个笔记独立生成Nonce

### 3.4 Google Drive备份模块

**职责**：定期将数据库导出并备份至Google Drive

**认证方式**：**服务账号（Service Account）**

选型理由：服务端自动化备份场景，无需人工交互OAuth授权

**备份流程**：
```
定时触发（每日凌晨2:00）
    ↓
执行数据库导出（pg_dump / sqlite3 .dump）
    ↓
生成 SQL 备份文件
    ↓
压缩为 .tar.gz 格式
    ↓
生成带时间戳的文件名（如 backup_2026-08-12_020000.tar.gz）
    ↓
调用 Google Drive API (Files.create) 上传
    ↓
记录日志（成功/失败）
    ↓
失败时触发告警通知
```

**所需Google Drive权限范围**：
```
https://www.googleapis.com/auth/drive.file
```
该范围仅允许应用访问自己创建或打开的文件，遵循最小权限原则。


## 4. 数据库设计

### 4.1 ER图

```
┌─────────────────┐          ┌─────────────────────────────────┐
│      users       │          │             notes               │
├─────────────────┤          ├─────────────────────────────────┤
│ id (PK)         │◄─────────│ user_id (FK)                    │
│ username        │          │ id (PK)                         │
│ email           │          │ title_encrypted (TEXT)          │
│ password_hash   │          │ content_encrypted (TEXT)        │
│ created_at      │          │ nonce (TEXT)                    │
│ updated_at      │          │ created_at (TIMESTAMP)          │
└─────────────────┘          │ updated_at (TIMESTAMP)          │
                             └─────────────────────────────────┘
```

### 4.2 表结构详细定义

**users 表**：

| 字段 | 类型 | 说明 |
| :--- | :--- | :--- |
| id | UUID | 主键 |
| username | VARCHAR(64) | 用户名，唯一 |
| email | VARCHAR(128) | 邮箱，唯一 |
| password_hash | VARCHAR(255) | bcrypt哈希密码 |
| created_at | TIMESTAMP | 创建时间 |
| updated_at | TIMESTAMP | 更新时间 |

**notes 表**：

| 字段 | 类型 | 说明 |
| :--- | :--- | :--- |
| id | UUID | 主键 |
| user_id | UUID | 外键，关联users.id |
| title_encrypted | TEXT | AES-256-GCM加密后的标题（Base64） |
| content_encrypted | TEXT | AES-256-GCM加密后的正文（Base64） |
| nonce | VARCHAR(64) | 加密使用的Nonce（Base64） |
| created_at | TIMESTAMP | 创建时间 |
| updated_at | TIMESTAMP | 更新时间 |

**索引策略**：
- `notes.user_id` 索引——加速按用户查询
- `notes.updated_at` 索引——加速列表排序


## 5. API设计

### 5.1 接口规范

- 基础路径：`/api/v1`
- 认证方式：`Authorization: Bearer <JWT>`
- 数据格式：JSON
- 统一响应格式：

```json
{
    "code": 0,
    "message": "success",
    "data": { ... }
}
```

### 5.2 核心API列表

| 方法 | 路径 | 描述 | 认证 |
| :--- | :--- | :--- | :--- |
| POST | `/auth/register` | 用户注册 | 否 |
| POST | `/auth/login` | 用户登录 | 否 |
| POST | `/auth/logout` | 用户登出 | 是 |
| GET | `/notes` | 获取笔记列表 | 是 |
| GET | `/notes/{id}` | 获取单篇笔记 | 是 |
| POST | `/notes` | 创建笔记 | 是 |
| PUT | `/notes/{id}` | 更新笔记 | 是 |
| DELETE | `/notes/{id}` | 删除笔记 | 是 |
| GET | `/notes/search?q={query}` | 搜索笔记 | 是 |


## 6. 部署架构

### 6.1 容器化部署方案

```
┌───────────────────────────────────────────────────────────────┐
│                      Docker Host                             │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │                   Docker Compose                        │  │
│  │  ┌───────────────┐  ┌───────────────┐  ┌────────────┐ │  │
│  │  │   nginx       │  │   backend     │  │  postgres  │ │  │
│  │  │   (容器)      │──│   (容器)      │──│  (容器)    │ │  │
│  │  │   :80/443     │  │   :8000       │  │  :5432     │ │  │
│  │  └───────────────┘  └───────────────┘  └────────────┘ │  │
│  │                           │                             │  │
│  │                           ▼                             │  │
│  │                    ┌───────────────┐                    │  │
│  │                    │   frontend    │                    │  │
│  │                    │   (容器)      │                    │  │
│  │                    │   :3000       │                    │  │
│  │                    └───────────────┘                    │  │
│  └─────────────────────────────────────────────────────────┘  │
│                              │                                │
│                              ▼                                │
│                    ┌───────────────────┐                      │
│                    │   Google Drive    │                      │
│                    │   (备份存储)       │                      │
│                    └───────────────────┘                      │
└───────────────────────────────────────────────────────────────┘
```

### 6.2 环境变量清单

| 变量名 | 说明 | 示例 |
| :--- | :--- | :--- |
| `DATABASE_URL` | 数据库连接字符串 | `postgresql://user:pass@db:5432/notes` |
| `JWT_SECRET` | JWT签名密钥 | 随机32字节字符串 |
| `ENCRYPTION_KEY` | AES-256-GCM主密钥 | 随机32字节（Base64） |
| `GOOGLE_DRIVE_CREDENTIALS` | 服务账号JSON凭证 | JSON字符串或文件路径 |
| `GOOGLE_DRIVE_FOLDER_ID` | 备份目标文件夹ID | `1abc123...` |
| `BACKUP_SCHEDULE` | 备份调度Cron表达式 | `0 2 * * *` |
| `LOG_LEVEL` | 日志级别 | `INFO` / `DEBUG` |


## 7. 安全架构

### 7.1 安全分层模型

```
┌─────────────────────────────────────────────────────────────┐
│                      安全分层模型                            │
├─────────────────────────────────────────────────────────────┤
│  第1层: 网络层   →  HTTPS加密传输 (TLS 1.3)                │
│  第2层: 认证层   →  JWT身份验证 + 会话管理                  │
│  第3层: 授权层   →  基于user_id的数据隔离                   │
│  第4层: 数据层   →  AES-256-GCM加密存储                    │
│  第5层: 密钥层   →  环境变量/KMS管理密钥                   │
│  第6层: 备份层   →  加密备份 + Google Drive安全存储        │
└─────────────────────────────────────────────────────────────┘
```

### 7.2 威胁模型与对策

| 威胁 | 对策 |
| :--- | :--- |
| 数据传输被窃听 | 全站HTTPS加密 |
| 数据库被拖库 | 笔记内容AES-256-GCM加密，无明文 |
| 密钥泄露 | 密钥与代码分离，定期轮换 |
| 未授权访问 | JWT认证 + 所有权验证 |
| SQL注入 | ORM/参数化查询 |
| 备份文件泄露 | 备份文件本身已是加密数据库 |


## 8. 项目结构

```
notes-app/
├── backend/                     # 后端服务
│   ├── app/
│   │   ├── api/                 # API路由
│   │   │   ├── auth.py          # 认证接口
│   │   │   └── notes.py         # 笔记接口
│   │   ├── core/                # 核心模块
│   │   │   ├── config.py        # 配置管理
│   │   │   ├── security.py      # JWT认证
│   │   │   └── encryption.py    # AES-256-GCM加密
│   │   ├── models/              # 数据模型
│   │   │   ├── user.py
│   │   │   └── note.py
│   │   └── services/            # 业务服务
│   │       ├── note_service.py
│   │       └── backup_service.py # Google Drive备份
│   ├── migrations/              # 数据库迁移
│   ├── tests/                   # 单元测试
│   ├── requirements.txt
│   └── Dockerfile
├── frontend/                    # 前端应用
│   ├── src/
│   │   ├── components/          # UI组件
│   │   │   ├── Auth/            # 登录/注册
│   │   │   ├── Notes/           # 笔记列表/详情/编辑
│   │   │   └── Editor/          # Markdown编辑器
│   │   ├── hooks/               # 自定义Hooks
│   │   ├── services/            # API调用
│   │   └── utils/
│   ├── Dockerfile
│   └── package.json
├── nginx/                       # Nginx配置
│   └── nginx.conf
├── docker-compose.yml           # 容器编排
├── .env.example                 # 环境变量模板
└── README.md                    # 项目文档
```


## 9. 开发与部署路线图

### 9.1 开发阶段

| 阶段 | 周期 | 核心交付 |
| :--- | :--- | :--- |
| **阶段1：基础架构** | 2周 | 项目骨架、Docker配置、数据库设计 |
| **阶段2：认证模块** | 1周 | 注册、登录、JWT认证 |
| **阶段3：笔记CRUD** | 2周 | 笔记增删改查、列表、搜索 |
| **阶段4：加密模块** | 1周 | AES-256-GCM加解密集成 |
| **阶段5：前端界面** | 2周 | 完整的Web UI（登录页、笔记列表、编辑器） |
| **阶段6：Google Drive备份** | 1周 | 备份脚本、定时任务、API集成 |
| **阶段7：测试与部署** | 1周 | 集成测试、生产环境部署 |

### 9.2 部署步骤

1. 配置Google Cloud Platform项目，启用Drive API，创建服务账号
2. 准备 `.env` 文件，填入所有环境变量
3. 执行 `docker-compose up -d` 启动所有服务
4. 配置Nginx SSL证书（使用Let's Encrypt）
5. 验证备份任务正常运行


## 10. 附录

### 10.1 参考项目与文档

- [Probably Important](https://github.com/joseeden/Probably-Important) —— 笔记应用规格参考
- [ENote](https://github.com/Ceche-Tulip/ENote) —— 笔记软件需求分析文档参考
- [Aether Notes](https://github.com/fujiDevv/aethernotes) —— AES-256-GCM加密实现参考
- [note-zero](https://github.com/yinguangxue/note-zero) —— 系统架构参考

### 10.2 术语表

| 术语 | 说明 |
| :--- | :--- |
| JWT | JSON Web Token，用于身份认证 |
| AES-256-GCM | 高级加密标准，256位密钥，伽罗瓦/计数器模式 |
| Nonce | 加密中使用的一次性随机数 |
| AEAD | 认证加密，同时提供机密性和完整性验证 |
| Service Account | Google Cloud服务账号，用于服务器间认证 |